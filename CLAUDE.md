# 沟通方式

和本项目的维护者交流时，用通俗易懂的语言，按面向初学者的方式解释。这条规则适用于所有对话，不只是代码相关的。

具体要求：

- **先说结论和影响，再说技术细节。** 先回答「这对我意味着什么」，再回答「技术上是怎么回事」。
- **技术名词先解释再用。** 第一次出现的术语（例如「并发」「重构」「迁移」「竞态」）先用一句大白话说明它指什么，再继续往下讲。
- **少用比喻和修辞，直接说事实。** 短句优先，不要为了文采绕弯子。
- **多举具体例子。** 讲一条抽象规则时，配一个真实场景：什么情况下会发生、会看到什么现象。
- **说清风险和代价。** 要动手改代码时，说明会不会影响现有功能、大概要改多少、出问题了怎么退回去。
- **默认用中文回答。**

反面例子（不要这样写）：
> 该架构的容错策略在系统性地制造它自身需要事后清理的漂移。

正面例子（应该这样写）：
> 现在的代码遇到删除失败时，只记一条日志就当作成功了。结果是：RAGFlow 里的文档还在，本地却把记录删了。这个文档就变成了「没人管的孤儿」，只有跑 Reconcile 才能发现它。
